services:
  app:
    build: .
    container_name: zayavki-app
    restart: unless-stopped
    ports:
      - "${PORT:-3005}:${PORT:-3005}"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
